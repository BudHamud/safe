const { parseExcelRow } = require('./src/app/components/movements.utils.js');
// Wait, it is typescript, let's just make a small test script using tsx or run with ts-node if available, or just read the function visually
